/**
 * @file Fixtures - tk
 * @module fixtures/tk
 */

/**
 * Token types.
 *
 * @enum {Lowercase<string>}
 */
enum tk {
  bigint = 'bigint',
  inlineTag = 'inlineTag',
  number = 'number',
  punctuator = 'punctuator',
  string = 'string',
  tag = 'tag',
  whitespace = 'whitespace'
}

export default tk
